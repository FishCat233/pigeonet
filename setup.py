from setuptools import setup, find_packages

setup(
    name="pigeonet",
    version="0.0.2",
    packages=find_packages(),
    install_requires = [
        "numpy"
    ],
    description="a simple package about deeplearning.",
    author="FishCat233",
    py_modules=[]
)
